<div class="container">
  <h4 class="col-xl-4 col-lg-5 col-md-7 py-1 mt-2 back-text"><strong><%= @category.category_name %>一覧</strong></h4>
    <div class="row">

      <% if @post_coffees.present? %>
        <%= render 'public/post_coffees/post_index' %>
      <% else %>
        <div class="mx-auto mt-5">
          <h3><strong><%= @category.category_name %></strong>の投稿はありません</h3>
        </div>
      <% end %>

  　</div>
</div>

<!--ページネーション-->
<div class="mx-auto">
  <%= paginate @post_coffees %>
</div>