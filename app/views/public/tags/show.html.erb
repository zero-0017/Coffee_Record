<div class="container">
  <h4 class="col-4 py-1 mt-2 text-center text-light" style="background-color:#000;"><strong><%= @tag.tag_name %>一覧</strong></h4>
    <div class="row">

      <% if @post_coffees.present? %>
        <%= render 'public/post_coffees/post_index' %>
      <% else %>
        <div class="mx-auto mt-5">
          <h3><strong><%= @tag.tag_name %></strong>の投稿はありません</h3>
        </div>
      <% end %>

  　</div>
</div>

<!--ページネーション-->
<div class="mx-auto mb-3">
  <%= paginate @post_coffees %>
</div>