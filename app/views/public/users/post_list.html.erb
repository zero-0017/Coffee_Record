<div class="container">
  <div class="row">
    <% if @user == current_user %>
      <h4 class="col-md-6 col-lg-4 col-xl-3 py-1 mt-2 py-1 mt-2 back-text shadow">
        <strong>
          会員の投稿一覧
        </strong>
      </h4>
    <% else %>
      <!--パソコンの表示-->
      <h4 class="col-md-12 col-lg-8 col-xl-6 py-1 mt-2 back-text shadow">
        <strong class="d-none d-md-block">
          <%= @user.name %>の投稿一覧
        </strong>

        <!--タブレット、スマホの表示-->
        <strong class="d-block d-md-none">
          <%= @user.name.truncate(8) %>の投稿一覧
        </strong>
      </h4>
    <% end %>

      <!--全体の数とページの数を表示-->
      <div class="col-sm-5 col-lg-6 col-xl-5 list-top-all mt-3">
        <%= page_entries_info(@post_coffees) %>
      </div>
  </div>

    <div class="row">
      <% if @post_coffees.present? %>
        <!--一覧一部テンプレート-->
        <%= render 'public/post_coffees/post_index' %>
      <% else %>

        <!--パソコンの表示-->
        <div class="mx-auto mt-5 d-none d-lg-block">
          <h3>
            <strong>
              投稿はありません
            </strong>
          </h3>
        </div>

        <!--タブレット、スマホの表示-->
        <div class="mx-auto mt-5 d-block d-lg-none">
          <h4>
            <strong>
              投稿はありません
            </strong>
          </h4>
        </div>

      <% end %>

  　</div>
</div>

<!--ページネーション-->
<%= render 'layouts/post_paginate' %>